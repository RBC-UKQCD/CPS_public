/*
 * Please do not edit this file.
 * It was generated by CPC make system.
 */

#include "alg/cg_arg.h"
CPS_START_NAMESPACE
	 CgArg::CgArg (char *file) {
	   Decode(file,"instance");
	 };
	 void CgArg::Encode(char *filename,char *instance){
		 VML vmls;
		 vmls.Create(filename,VML_ENCODE);
		 Vml(&vmls,instance);
		 vmls.Destroy();
	 }

	 void CgArg::Decode(char *filename,char *instance){
		 VML vmls;
		 vmls.Create(filename,VML_DECODE);
		 Vml(&vmls,instance);
		 vmls.Destroy();
	 }
	 void CgArg::Vml(VML *vmls,char *instance){
		 vml_CgArg(vmls,instance,this);
	 }


bool_t
vml_CgArg (VML *vmls, char *name,CgArg *objp)
{
	register int32_t *buf;

	 vml_class_begin(vmls,"CgArg",name);
	 if (!vml_Float (vmls, "mass", &objp->mass))
		 return VML_FALSE;
	 if (!vml_int (vmls, "max_num_iter", &objp->max_num_iter))
		 return VML_FALSE;
	 if (!vml_Float (vmls, "stop_rsd", &objp->stop_rsd))
		 return VML_FALSE;
	 if (!vml_Float (vmls, "true_rsd", &objp->true_rsd))
		 return VML_FALSE;
	 if (!vml_RitzMatType (vmls, "RitzMatOper", &objp->RitzMatOper))
		 return VML_FALSE;
	 vml_class_end(vmls,"CgArg",name);
	return VML_TRUE;
}
CPS_END_NAMESPACE
